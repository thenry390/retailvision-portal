# RetailVision v0.4.0 — Sprint 4

## API Integration and Service Layer
- Added a centralized asynchronous API client.
- Added domain services for all implemented product modules.
- Added a shared service-backed data provider.
- Removed direct mock-data imports from pages.
- Added loading skeletons, normalized errors, and retry behavior.
- Added an API failure simulation using `?apiError=1`.
- Added REST contract and Sprint 4 backlog documentation.

## Verification
- TypeScript project compilation completed successfully.
- A full Vite production bundle could not be executed in the packaging environment because its npm mirror lacked Rollup's Linux optional binary. Run `npm install` and `npm run build` on Windows to complete release verification.
